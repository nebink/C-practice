#include <stdio.h>
int main()
{
    int a;
    printf("Enter the no. of values to inserted in the array :");
    scanf("%d",&a);
    int (arr[a]);
    for(int i; i<a; i++){
        printf("\nenter the values");
        scanf("%d",&arr[i]);
    }
    
    //to print the array
    printf("\nThe inserted array is :");
    
for (int j ;j<a; j++){
    printf("\n %d",arr[j]);
        
}
    return 0;
    
}