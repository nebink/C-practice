#include <stdio.h>
int main()
{
    int a;
    printf("Enter the no. of values to inserted in the array :");
    scanf("%d",&a);
    int (arr[a]);
    for(int i; i<a; i++){
        printf("\nenter the values");
        scanf("%d",&arr[i]);
    }
    
    return 0;
}